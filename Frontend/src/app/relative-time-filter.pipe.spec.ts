import { RelativeTimeFilterPipe } from './relative-time-filter.pipe';

describe('RelativeTimeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeTimeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
